
# Modern Finance App


Local development copy of the app project. This clone is prepared for pushing to GitHub.

- Repo prepared on: 2025-11-18
- Branch: `feature/local-edits` (existing)

How to add your GitHub remote and push:

```powershell
# add remote (example)
git remote add origin https://github.com/<your-username>/<repo>.git
# push main branch
git branch -M main
git push -u origin main
# push feature branch
git push -u origin feature/local-edits
```

If you want me to add the actual remote and push, provide the GitHub repository URL and I will run the push commands for you.